float4 VS(float3 p : POSITION, 
          float4 c : COLOR,
		  float2 t : TEXTURE ) : SV_POSITION
{
	float3 vPos = p;
	return float4(vPos.x,vPos.y, vPos.z, 1.0f); 
}
float4 PS(float4 p : SV_POSITION) : SV_Target
{
	return float4(0,0,1,1);
}